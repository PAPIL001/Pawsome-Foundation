{
    "name": "pawsome-foundation-root",
    "version": "1.0.0",
    "description": "Root package for Vercel deployment of Pawsome Foundation React App",
    "private": true,
    "scripts": {
        "build": "cd client && npm install && npm run build",
        "start": "cd client && npm run preview"
    }
}